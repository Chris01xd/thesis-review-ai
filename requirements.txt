# ThesisReview AI — Backend FastAPI
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9

# Documento y PDF
python-docx>=1.1.2
PyPDF2>=3.0.1
reportlab>=4.2.5

# HTTP y APIs externas
requests>=2.32.3
httpx>=0.27.0

# IA
openai>=1.40.0
scikit-learn>=1.3.0

# Detector de IA — transformers es liviano; torch NO se incluye aquí (>1 GB, supera RAM de Render free)
# Para activar el modelo HuggingFace localmente:
#   Windows: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   Linux:   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Sin torch el detector funciona en modo estadístico (fallback automático).
transformers>=4.40.0

# Seguridad y utilidades
python-dotenv>=1.0.1
bcrypt>=4.2.0

# PostgreSQL (producción en Render)
psycopg2-binary>=2.9.9
